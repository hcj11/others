             根据图表,从平均值和最慢,最快三个方面进行统计,来看,

0. 采用10组  ，每组做10个循环的  四种数据交换的程序进行测试  测试方式,可以减少偶然性.

1. 从4组调查结果中可以看出来,
barrier 、synchronsQueue 、exchangers 的曲线在平均最快和最慢的分析上看，都是平稳升降的，区别不大
2. phasers 在保证数据的安全性交换的方式下，从最快，最慢，平均值上看都是性能最差的，尝试  在保证安全性的前提下的提高性能。


***综上,采用 barrier 、synchronsQueue 、exchangers  的方式会是好的方式,***

3. 优化方向： 
	
	3.1  算法设计和优化

